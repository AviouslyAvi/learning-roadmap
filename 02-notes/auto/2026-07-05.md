---
date: 2026-07-05
type: auto-log
generated: 2026-07-06
---

# 2026-07-05 — daily activity

A heavy build day almost entirely on **remoter-recording** — this was the Phase 2 push. The morning cleared a backlog of merges (pre-flight detectors, monitoring, host-info/clock-sync, idempotency keys, schema-drift fixes), then the afternoon and evening went into the real infrastructure: a live `wss://` signaling client to replace the null transport seam, in-plugin login + session picker replacing env creds, real email transport via Resend, and finally an M2 media engine wiring real libwebrtc/Opus off the audio thread. On the side, **Watch-Party** got a full Apple-style UI facelift shipped through v0.8.10 (new theme set, backdrop blur, floating iMessage-style chat panel), and **chord-inversion-trainer** picked up a shared prompt-conveyor component with per-unit free practice and timers. Note: all of yesterday's remoter-recording commits live on `claude/*` feature branches — local and `origin/main` are still at `bca51be`, so nothing's merged to main yet.

## By workspace

### Learning Roadmap
- `README.md`, `06-weekly/2026-27.md`, `06-weekly/2026-26.md` — touched by the prior nightly job (weekly-digest backfill + README log section); no hand edits.

### Projects

**remoter-recording** — Phase 2 (all on `claude/*` feature branches; not merged to main)
- `727e7f8` M2: real libwebrtc/Opus media engine + off-audio-thread go-live wiring
- `c91885a` M2 fixup: route ProcessBlock's DAW audio through the monitor, not the OS mic
- `f9fd3d7` M2 WIP: exploratory two-peer Opus/WebRTC smoke test (not wired into build)
- `bcabe47` Add WIP handoff for the M2 libdatachannel exploration
- `1355139` Merge Phase 2: O1 + U1 + M1
- `12e0c27` M1: real `wss://` signaling client (replace the C8 Null transport seam)
- `9669ec9` U1: in-plugin login + session picker (replaces env creds)
- `641e2c6` O1: wire real email transport (Resend) for verify + password-reset
- `fc2149e` Phase 2 roadmap + DAW verification runbook
- `756afed` D5: sessions e2e + ESLint + doc-drift fixes
- Morning merge batch: `6c844b9` C8 live monitor · `112ce38` C7 host_info probe + clock_sync + AU/CLAP breadth · `25fcf9a` C5 clip/silence/input-present pre-flight detectors · `63010b5` S3 Idempotency-Key on create/finalize · `e89d1bc` D3 schema single-source equivalence check · `81917e7` D4 argon2→scrypt + preflight camelCase · `493830f` D2 host_info wiring (PRs #18–#23 merged)

**Watch-Party** — Apple-style UI refresh, shipped to `main`
- `a47ebbb` feat(client): float the chat panel as an inset card (v0.8.10)
- `cdf9c49` feat(client): Apple-style panel shell + iMessage chat (v0.8.9)
- `d0abb03` fix(client): clamp onboarding tooltip to viewport
- `461e73e` feat(client): new Apple-style theme set + per-theme backdrop blur
- `4777f51` chore(release): v0.8.8 (tags v0.8.8 → v0.8.10)

**chord-inversion-trainer** — practice-flow components, on `master`
- `bfc84d9` Add recap-ribbon conveyor to Learn Chord Types
- `fb495e2` Add per-unit Free Practice, timers, and conveyor integration
- `66cfa58` Add shared prompt-conveyor component + styles
- `ebea6ee` Whole-course UX fixes: accuracy stars + C-only labels
- `e587942` Update handoff: recap ribbon done in Learn Chord Types
