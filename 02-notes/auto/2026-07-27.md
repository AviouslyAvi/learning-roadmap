---
date: 2026-07-27
type: auto-log
generated: 2026-07-28
---

# 2026-07-27 — daily activity

Big day on remote-recording — twenty commits and the M3 mesh finally went live with the audio fix confirmed by ear. The thread ran: restore the self-clocking pump (crackle gone), then carry the monitor path in stereo end to end, kill APM's high-pass filter, pin bitrate at 128 kbps, and surface whether the ICE candidate pair is direct or relayed. Late in the day I chased an arm-gate bug through two wrong root causes before landing on the real one — the gate was only watching channel 0 instead of every input channel. Also ran a full resume + cover letter build through the resume kit for one application, and touched up the kit's skills taxonomy and project experience along the way.

## By workspace

### Projects

**remote-recording** — 27 source/doc files touched, 20 commits (several still on local branches, not yet pushed).

Monitor / audio path:
- `ce58ec8` fix(monitor): restore self-clocking pump — crackle confirmed fixed on live mesh
- `d3c2a54` feat(monitor): carry the monitor path in stereo end to end
- `79dfd7c` fix(monitor): kill APM's high-pass filter; one bitrate constant at 128 kbps
- `b67cf65` / `14f1f7b` feat(monitor): surface the selected ICE candidate pair — direct vs relayed
- `5ef2122` wip(monitor): port session 23b/24 working tree (stereo path + quality fixes)
- Files: `apps/plugin/monitor/{MonitorSession,WebRtcMonitorMediaEngine}.{cpp,h}`, `MonitorMixer.h`, `AudioRingBuffer.h`, `IMonitorMediaEngine.h`

Arm gate / UI:
- `fa47e2d` fix(ui): explain the arm gate by role — receive-only is not "no input" *(on `stream-b/arm-role-clarity`)*
- `32ac3a3` fix(plugin): arm gate must watch every input channel, not just channel 0 *(on `fix/arm-gate-multichannel-input`; not on main)*
- Files: `apps/plugin/detect/InputMonitor.{cpp,h}`, `apps/plugin/ui/BridgePanel.h`, `apps/plugin/RemoterBridge.cpp`

Tests / tooling:
- `7c6712a` test(m1): bring ws_transport_it up to the M3 id-ordering role model
- `f159f2d` test(m1): link ClockSync.cpp into the ws integration binary *(on `bisect/revert-pump-ddefc9d`)*
- Files: `apps/plugin/tests/{monitor_control_test,audio_ring_buffer_test,monitor_mixer_test,ws_transport_it,detect_test}.cpp`, `scripts/analyze-monitor-dropouts.py`, `services/signal/scripts/m1-ws-it.mjs`, `apps/plugin/scripts/bootstrap-resources.sh`

Docs / handoffs:
- `bdd832f` docs(m3): phase 6 — the mesh is live and the audio fix is confirmed by ear
- `479e15f` docs(m3): post-merge handoff — next steps after Phase 6 went live *(this is `origin/main`)*
- `d3556ea` docs(m3): session 26 — session 25's root cause was wrong; the gate was never closed
- `81ff532` docs(m3): session 25 — the rig test never ran; same-user supersede loop
- `4723248` docs(m3): the rig's installed bundles were clobbered — Stream D was missing
- `70ecefd` docs(m3): session 23b/24 — stereo path, APM high-pass finding, ICE path badge
- `1bc118f` docs(m3): session 23 — pump restored, all 4 targets built, crackle fixed
- `86fed85` / `f3645e0` docs: VPS2 `/auth/register` email-transport fix prompt *(tagged `backup/pre-split-2026-07-27`)*
- `b381164` chore: ignore `.claude/rig-backup-*/` plugin bundle snapshots
- Files: `ROADMAP.md`, `HANDOFF-MAC-BUILD.md`, `HANDOFF-M3-POST-MERGE.md`, `WINDOWS-NODE-HANDOFF.md`, `STEP-A-LAUNCH-BLOCKS.md`, `VPS2-FIX-REGISTER-PROMPT.md`, `.gitignore`

API:
- `services/api/src/auth/service.ts` — auth service touched (email transport thread)

### Learning Roadmap
- `README.md` — weekly log section refreshed by the nightly job
- `06-weekly/2026-30.md` — week 30 digest written
- `10f7d42` auto: weekly digest 2026-30 + daily log 2026-07-25

### Other
- **claude-resume-kit** — 19 files touched. Full end-to-end resume + cover letter build for one target (LaTeX → PDF, both documents), plus edits to `resume_builder/support/skills_taxonomy.md`, `resume_builder/experience/experience_projects.md`, `config.md`, and `CLAUDE.md`. No commits.

### Job search
- 4 files touched — one new application folder (posting capture, JD, tailored resume + cover letter PDFs).
