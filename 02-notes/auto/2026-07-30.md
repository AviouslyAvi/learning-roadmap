---
date: 2026-07-30
type: auto-log
generated: 2026-08-02
backfilled: true
---

# 2026-07-30 — daily activity

Spent the day getting a packaged v4 alpha out the door for a real cross-country remote monitor test. Set that as the explicit goal, then cleared the path to it: rig-verify passed on the `5eb9f28` build (session 34), pruned the remote down to `main` plus two open-PR branches, and rebuilt off main for clean provenance — which surfaced a `__DATE__` md5 gotcha worth remembering, since the embedded build date makes otherwise-identical builds hash differently. Two real plugin fixes along the way: null-checking per-channel input pointers in the monitor paths, and stopping WDL's UTF-8 macros from mangling `std::fopen`/`strftime` in `Log.h`. Also linked the Win32 system libs a WEBRTC-ON build needs. Concluded that relay-only is a one-liner and the tester is live, so it's worth going straight at the real test rather than building more scaffolding first.

> Backfilled on 2026-08-02. Reconstructed from git history only — file-modification data for this day is no longer recoverable, so non-git activity (Clients, Job-Search, General Research, loose notes) is not represented.

## By workspace

### Projects

**remote-recording**
- `5535a57` — docs(m3): session 34 rig-verify PASSED — both checks confirmed on 5eb9f28 build
- `3879eed` — docs(m3): new goal — cross-country alpha for a remote monitor test
- `08bb05b` — docs(m3): record branch-pruning decision and ready-to-run cleanup command
- `46eb047` — docs(m3): branch prune complete — remote is main + 2 open-PR branches
- `812d924` — docs(m3): rebuild off main (5eb9f28) for provenance; record `__DATE__` md5 gotcha
- `1abee7a` — fix(plugin): null-check per-channel input pointers in monitor paths
- `caa9c78` — fix(plugin): stop WDL's UTF-8 macros mangling std::fopen/strftime in Log.h
- `dd29fd5` — build(plugin): link the Win32 system libs a WEBRTC-ON build needs
- `9fa3021` — chore(gitignore): ignore releases/ for packaged alpha builds
- `a6f5040` — docs(m3): session 38 — v4 alpha rebuilt, verified, packaged
- `6a59560` — docs(m3): relay-only is a one-liner; tester is live so go straight at the real test
