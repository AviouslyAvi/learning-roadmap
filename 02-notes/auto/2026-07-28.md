---
date: 2026-07-28
type: auto-log
generated: 2026-07-29
---

# 2026-07-28 — daily activity

Heavy day on remote-recording — 18 commits between 20:50 and 23:46, almost all of it chasing WebRTC reliability in the RemoterBridge plugin. The big win was realizing the negotiation was edge-triggered and rewriting it to be level-triggered, which the new diagnostic log file confirmed as the toggle-cycle root cause. Two more real bugs fell out of that session: a 403 that turned out to be a shared upload spool rather than an auth problem, and an API error envelope we weren't unwrapping. Two PRs (#41, #42) merged, a build got deployed to the rig at 23:45, and the notes explicitly say the next session should be planning, not coding. A short burst of client work in the morning; nothing else moved.

## By workspace

### Projects

**remote-recording** — WebRTC reliability push (milestone 3)

Source touched: `apps/plugin/RemoterBridge.cpp/.h`, `apps/plugin/monitor/MonitorSession.cpp/.h`, `apps/plugin/upload/{UploadQueue,UploadJournal,ApiClient}`, `apps/plugin/log/Log.h`, `apps/plugin/ui/{BridgePanel.h,UiState.h}`, five test files under `apps/plugin/tests/`, plus `CMakeLists.txt`, `.github/workflows/plugin.yml`, `.gitignore`, and `HANDOFF-WEBRTC-RELIABILITY.md`.

Commits (all present on both `main` and `origin/main`):

- `c6198f2` docs(m3): handoff — make WebRTC negotiation level-triggered
- `231a46b` feat(plugin): a diagnostic log file + the negotiation decision trail
- `dd4c39e` docs(m3): the log landed and it confirmed the toggle-cycle root cause
- `3a0f65b` fix(plugin): make WebRTC negotiation level-triggered
- `937cfae` docs(m3): §2 landed — level-triggered negotiation, and the arm question is open
- `a9c01c4` docs(m3): §2 confirmed on the rig, and §10 turns out to be routing
- `f9c98b7` fix(plugin): scope the upload spool to the take's owner
- `a264050` fix(plugin): read the API's nested error envelope
- `6597d54` docs(m3): §4 answered — the 403 was a shared upload spool, not auth
- `a7f7fd1` docs(m3): the 22:08:40 build — §4's fixes are on the rig
- `8c0ec98` docs(m3): §4 closed outright — the takes were never stranded
- `e15a6b4` Merge pull request #41 from AviouslyAvi/fix/arm-gate-multichannel-input
- `6e83420` docs(m3): PR #41 is merged — point the next session at main
- `0d32118` Merge pull request #42 from AviouslyAvi/docs/handoff-post-merge-41
- `10bf64e` docs(m3): next session is a planning session, not a coding one
- `1032753` feat(ui): surface a superseded signaling socket on the panel
- `adbdae5` docs(m3): record #5 built, #4 reassessed, housekeeping already done
- `24aa64d` docs(m3): the 23:45:20 build is deployed — record it and the verify procedure

Also: three `rig-backup-2026-07-28-*` snapshots under `.claude/` (preLog, preSpoolOwnership, preSupersedeUi), and two out-of-tree build dirs (`build-verify/`, `build-webrtc/`).

*Note: local `main` is currently ahead 4 / behind 2 vs `origin/main` — worth a `git pull` before the next session.*

### Client work

Client work — 4 files touched in 1 client folder (a decision record, two planning docs, and an update to a call-reference guide).

### Other

- `.claude/settings.local.json` in this repo — local permissions tweak, not tracked.
