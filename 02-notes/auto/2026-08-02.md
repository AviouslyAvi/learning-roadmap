---
date: 2026-08-02
type: auto-log
generated: 2026-08-03
---

# 2026-08-02 — daily activity

Huge day on RemoterBridge — 42 commits, essentially the whole of Phase 2.1–2.5 landed. The through-line was timing: bringing `masterPositionSamples` home in the take manifest, persisting raw `armWallMs`, correcting clock_sync placement against a calibrated loopback, and adding burst-on-connect + MAD rejection + a warm-up gate so placement doesn't fire on garbage. Alongside that, a full day of CI firefighting — VS 2026 broke the hard-pinned CMake generator, two high-severity Fastify transitives were blocking, and the Windows installer path turned out to have never actually been run (three blockers). Ended the evening documenting the Windows rig's real state: two different builds deployed at two paths, which explains a lot. Local `main` is 9 commits behind `origin/main` — worth pulling before the next session.

## By workspace

### Projects

**remote-recording (RemoterBridge)** — 42 commits, ~48 files touched outside worktrees.

Phase 2 feature work:
- `4fb4694` / `092e452` feat(c1): bring masterPositionSamples home — Takes/manifest.json (#51)
- `9210c11` fix(c1): refuse a manifest row when the session sample rate is unknown
- `8c68e3e` / `5188091` feat(2.2): persist the raw armWallMs per take (#52)
- `46b4b1e` / `8f4350e` feat(2.1,2.5): correct clock_sync placement by the calibrated loopback
- `5a25970` / `c186cbf` feat(clock): burst on connect, MAD reject, and a warm-up gate on placement (Phase 2.3–2.5) (#54)
- `33f1bff` fix(clock): the warm-up ceiling never opens on a sub-millisecond path
- `2d2112c` feat(opt1): timeline-stamped stem export — drop at bar 1
- `39455d9` / `210be63` fix(plugin): arm on a CONNECTED input, not on a live level probe (#55)
- `6629b1f` feat(m3): instrument the startup window — per-second neteq + mon.startup
- `b42b186` feat(config): log whether config.env was actually read
- `05eaace` feat(monitor): REMOTER_FORCE_RELAY to force ICE through TURN

CI / build / release:
- `210feab` fix(ci): unpin the Windows CMake generator — VS 2026 broke the hard pin
- `d787547` fix(ci): restore the Windows plugin build — VS 2026 image + CMake 4.x strictness (#59) *(on origin/main; local not pulled)*
- `add1dde` fix(plugin): normalize IPLUG2_DIR to a CMake path
- `324a7e2` fix(deps): clear the two high-severity Fastify transitives blocking CI (#56)
- `0f1a5c4` fix(installer): the Windows packaging path had never been run — 3 blockers
- `d2d8f55` / `9e07300` build(release): repackage the macOS alpha as v5 from current main (#53) *(on origin/main; local not pulled)*

Merges / integration:
- `2df16d6` Merge origin/claude/clock-sync-warmup-gates-3cf80d into integ/phase21-base
- `f095403` Merge origin/claude/persist-armwallms-per-take-72fe2d into integ/phase21-base
- `d4ca5b7` Merge main (#51, #52) — keep both clock_sync metadata fields

Docs / handoffs:
- `0daea75` docs: handoff for Option 1 (timeline-stamped stem export)
- `dc6246d` docs: handoff for the force-relay result
- `4e934dc` docs: handoff for the frozen Info.plist version
- `8cc0191` docs: mark the Info.plist version handoff done, not pending (#62) *(on origin/main; local not pulled)*
- `984d762` docs: correct the CI landmine — red is real signal again
- `c030b73` docs: stop asserting why CI is red — teach the discriminator instead (#60) *(on origin/main; local not pulled)*
- `3f3f076` docs: STEP 1 fixed and merged (39455d9); STEP 2 done; CI is not billing-blocked
- `af39274` docs: mark PLAN-NEXT-PHASE Phase 2.2 done
- `d0d1686` docs: 0005 is applied on VPS2
- `0930bb6` docs: retire the stale "main is unpushed" blocker
- `778c641` docs(dmg): warn testers that a leftover 1.0.0 build silently outranks this one
- `225d2c0` docs: surface the force-relay Windows state on main *(on origin/main; local not pulled)*
- `1c477cf` docs: PR #50's installer commit is superseded — record the merge plan *(on origin/main; local not pulled)*
- `4729468` docs(windows): record the rig's actual state — build, installer, what's left *(on origin/main; local not pulled)*
- `d9a698b` docs(windows): note both alpha halves now come from 6629b1f *(on origin/main; local not pulled)*
- `81a33e2` docs(windows): the rig has TWO different builds deployed, at two paths *(on origin/main; local not pulled)*

Files touched (selection):
- `apps/plugin/monitor/ClockSync.{cpp,h}`, `MonitorSession.{cpp,h}` — clock sync warm-up + burst logic
- `apps/plugin/capture/TakeMetadata.{cpp,h}`, `apps/plugin/download/Take{Collector,DownloadStep,Manifest}.*`, `DownloadJournal.*` — take manifest + download path
- `apps/plugin/upload/{ApiClient,ApiUploadStep,TakeUploader,UploadJournal}.*` — upload path
- `apps/plugin/tests/{clock_sync,calibrate,detect,monitor,download_queue,upload_queue}_test.cpp` — test coverage across the new paths
- `services/api/migrations/0005_take_arm_wall_ms.sql`, `0006_take_clock_quality.sql`, `src/routes/takes.ts`, `src/takes/service.ts`, `src/mappers.ts` — API + schema for the new timing fields
- `tools/windows/{package-windows-installer.ps1,RemoterBridge.iss}`, `.github/workflows/plugin.yml` — Windows packaging + CI
- `docs/{signaling-protocol,api-contract,data-model}.md`, `db/schema.sql`, `PLAN-NEXT-PHASE.md`, `HANDOFF-M3-POST-MERGE.md`, `handoffs/HANDOFF-DAW-INGEST-OPT1-STAMPED-STEMS.md`

Note: 14 Claude worktrees under `.claude/worktrees/` were also created/refreshed during the day; their file churn is excluded above as duplicate.

### Learning Roadmap

- `.claude/settings.local.json` — permissions update
