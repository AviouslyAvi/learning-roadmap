---
date: 2026-07-03
type: auto-log
generated: 2026-07-05
---

# 2026-07-03 — daily activity

Deployment day for **remoter-recording** — the infrastructure went live. Stood up coturn on VPS1 (verified live) with TURNS/TLS on :5349 at signal.imavious.org and automatic cert sync; deployed VPS2 (API + Postgres) with api.imavious.org live behind Caddy (Let's Encrypt cert issued, healthz/readyz returning 200) and minted TURN creds. On the protocol side, R3 added the clock-sync handshake (clock_ping/clock_pong) on VPS1 signaling, C2 implemented loopback/PDC calibration with a pre-flight gate, C6 wired the plugin to the VPS2 R2 upload flow (upload-url → PUT → finalize), and D2 wired host_info persistence. A cluster of PRs (#8–#13) merged. 170 files touched (includes compiled `dist/` output).

## By workspace

### Projects

**remoter-recording** — VPS1/VPS2 live, R3 clock-sync, C2 calibration, C6 upload wiring
- `58e5068` — R1: stand up coturn on VPS1 (live + verified) + stage signaling
- `8a65a1c` — R1: add TURNS/TLS on :5349 (signal.imavious.org) + cert auto-sync
- `bc79ccd` — S4 + R4: deploy VPS2 (API + Postgres), bring VPS1 signaling online, mint TURN creds
- `27164a3` — S4: api.imavious.org live (Caddy LE cert issued, healthz/readyz 200)
- `06533a0` — R3: clock-sync handshake (clock_ping/clock_pong) on VPS1 signaling
- `4088ee0` / `bef913a` — Implement C2 — Loopback/PDC calibration + pre-flight gate (+ fixup)
- `cf451bf` — C6: wire the plugin to the VPS2 R2 upload flow (upload-url → PUT → finalize)
- `f06ce4a` — D2: host_info wiring (migration + shared type + mapper + arm-time persist)
- `88ff469` — Fix R2_BUCKET default to match the provisioned bucket
- merged PRs: #8 (R1-vps1), #9 (kickoff-vps2), #10 (S4-vps2-deploy), #11 (R3-clock-sync), #12 (fix-r2-bucket), #13 (C2-loopback-calibration)
- deploy configs: `deploy/vps1/{coturn,signal,caddy,db-tunnel}`, `deploy/vps2/{api,caddy,db}`
