---
date: 2026-07-13
type: auto-log
generated: 2026-07-14
---

# 2026-07-13 — daily activity

Big day on the music-tutor guitar app — I wired the MIDI engine straight into the fretboard tutor so a played key now lights every matching fret across all six strings, then built a MIDI-driven drill mode where `/test` judges answers by the exact sounding pitch instead of clicks (with click still working as a fallback in both modes). Also scoped out phase-2 mic pitch detection so a real guitar into the mic can reuse the same MIDI-judging code path — spec only, nothing built yet. Three new handoffs (12, 13, 14) capture all of it; the work is verified locally but not yet deployed or committed. A separate LumoraIT client thread also saw activity in a worktree.

## By workspace

### Projects

**music-tutor** (uncommitted working changes — verified locally, not deployed/committed)
- `handoffs/12-guitar-midi-wired.md` — MIDI engine copied from the piano app into `fretboard-tutor`; a played note now lights every matching fret across all six strings and shows the note name. Notes why it was copied verbatim rather than shared as a workspace package (repo isn't actually a pnpm monorepo).
- `handoffs/13-guitar-midi-drill.md` — `/test` drill gains an "Answer with" toggle (Click vs MIDI keyboard); MIDI answers judged by exact sounding MIDI pitch, using the guitar string's octave to disambiguate. Click stays wired as fallback.
- `handoffs/14-mic-pitch-detection-scope.md` — spec for phase-2 mic input (play the note on a real guitar into the mic); reduces to the existing `handleMidiNote` path via a pitch detector. Spec only, no code yet.
- `app/fretboard-tutor/` — Next.js build/cache artifacts and a `.vercel/project.json` regenerated from local dev/build runs.

### Client work
- Activity in one client folder (worktree) — reference notes, a decision doc, and a printer-recommendation document (`.md` + generated `.docx`/`.pdf`). Counts only.
