---
type: weekly-digest
week: 2026-29
generated: 2026-07-19
---

# Week 2026-29

## Themes
- **remote-recording went from hardening to go-live to live features.** The week arced from operational baselines (observability + safety) → production go-live on `api.imavious.org` → first Windows compile → shipping live presence. It's now a real, deployed, multi-platform app.
- **Music tooling on two fronts.** Early week was deep in the music-tutor / fretboard-tutor guitar app (MIDI wiring, drill mode, UI polish); late week pivoted to the audio-plugin layer, cloning the iPlug2 C++ framework to feed the VST projects.
- **Personal-Spending-Dashboard as the recurring side project.** Worktree-driven experiments (trip comparison, PDF extraction, CSV import) kept surfacing throughout the week.
- **Solo-builder cadence.** Almost entirely project work — no research, minimal client/job-search activity — with handoff docs doing the heavy lifting for cross-session continuity.

## Key learnings
- Judging a note drill by exact sounding MIDI pitch (using the guitar string's octave to disambiguate) rather than by clicks — with click retained as a fallback path.
- Production hardening as discrete baselines: O2 = observability (request-id correlation, log redaction, `/metrics`); O3 = operational safety (global rate limit, security headers, body size limit).
- Cross-platform native builds: got RemoterBridge compiling on Windows for the first time, and recorded the macOS standalone build as done.
- The macOS "app is damaged" quarantine problem for ad-hoc-signed Electron apps, plus tooling (`fix-quarantine.command`) to work around it.
- Copy-vs-share tradeoff: the MIDI engine was copied verbatim from the piano app into the guitar app rather than shared as a package, because the repo isn't actually a pnpm monorepo.

## Projects touched
- **remote-recording** — the week's main thread (hardening, go-live, Windows build, live presence)
- **music-tutor / fretboard-tutor** — MIDI drill + UI polish (early week)
- **iPlug2** — fresh clone, framework exploration (late week)
- **Personal-Spending-Dashboard** — trip-comparison + PDF/CSV experiments (worktrees)
- **ableton-extensions** — ArtRoll preview bridge tinkering
- **productivity-app** — macOS quarantine fix

## Shipped / committed
- **remote-recording shipped live** on `api.imavious.org` — O2/O3 hardening merged (PRs #30, #31) and VPS2 redeploy verified.
- **Live presence feature** merged (PR #33) — attendee rail lights up "You", flags the engineer, heartbeats others.
- **First Windows compile** of the RemoterBridge plugin (on `origin`, unpulled locally).
- **fretboard-tutor MIDI work** — `midiHighlights` prop + accidentals-both-input merged (PR #4).
- Plugin fix: record button arms in `clock_sync` mode on input alone.

## Open threads & blockers
- Several remote-recording commits sit on `origin`/worktree branches unpulled into local `main` (Windows build especially) — local repo is behind.
- **iPlug2** is cloned but untouched — no code written against it yet; intent (chainhost-vst / premonition-vst?) not yet acted on.
- **productivity-app** quarantine fix is on branch `claude/macos-quarantine-fix`, not merged to main.
- **music-tutor** phase-2 mic pitch detection is spec-only (handoff 14) — nothing built.
- No daily auto-log exists for **2026-07-17** (job didn't run or no activity captured) — a gap in the week's record.
