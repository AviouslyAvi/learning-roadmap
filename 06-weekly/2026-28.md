---
type: weekly-digest
week: 2026-28
generated: 2026-07-12
---

# Week 2026-28

## Themes
- **remoter-recording dominated the week** — the audio plugin moved from first interactive testing through a real media engine, a full UI reskin, and into M2 go-live prep. Nearly every commit-day touched this project.
- **Milestone velocity, then stabilization.** The big features (real libwebrtc/Opus engine, UI reskin) landed early/mid-week; the back half was about consolidating merges and restoring build state after the reskin merge dropped the engine.
- **Client deliverables ran quietly alongside** — proposal/estimate docs and cold-calling prep touched early in the week, no code.

## Key learnings
- Swapping in a real libwebrtc/Opus monitor engine means go-live has to move **off the audio thread** — a real-time-safety constraint, not just a plumbing change.
- Merges that reskin/restructure can silently **drop build wiring** (the engine build vanished after the reskin merge and had to be restored) — a reminder to build-validate right after big merges.
- JUCE/AU gotchas piled up: unsigned deploys + class-data mismatches fail AU load; `ProcessBlock` must guard against null channels in passthrough; fake test engines must keep implementing new interface methods (`IMonitorMediaEngine`) as the real one grows.
- Upload-path resilience patterns: refresh the token on a 401 and tolerate an unplaced `clock_sync` finalize rather than hard-failing.

## Projects touched
- **remoter-recording** — first interactive U1 testing, real media engine (#26), upload fixes (#28), UI reskin (#29), crash fix, M2 go-live runbook + engine-build restore. PRs #26/#27/#28/#29 all merged.

## Shipped / committed
- `#26` — real libwebrtc/Opus media engine + off-audio-thread go-live (→ main)
- `#27` — plugin standalone + login-panel UI fixes
- `#28` — U1 upload path: refresh-on-401 + clock_sync finalize
- `#29` — UI reskin (→ main)
- `8397abb` — restore WebRTC engine build after reskin merge dropped it (M2 go-live live-test branch)

## Open threads & blockers
- **M2 go-live** is the live edge: a two-machine live-test runbook exists and the engine build was just restored, but the local `main` is behind several `origin/claude/*` branches (M2 work lives on remotes not yet pulled).
- No Learning Roadmap, research, or job-search progress this week — remoter-recording absorbed all the focus.
