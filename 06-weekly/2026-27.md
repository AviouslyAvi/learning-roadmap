---
type: weekly-digest
week: 2026-27
generated: 2026-07-05
---

# Week 2026-27

> Days 06-29 → 07-04 covered (07-05 is the run date). Daily notes for 06-29 → 07-03 were backfilled 2026-07-05 after a nightly-log gap.

## Themes
- **remoter-recording went from scaffold to live production** — the week's dominant arc: VPS1 (coturn/TURN + signaling) and VPS2 (API + Postgres) both stood up and serving real HTTPS endpoints.
- Concurrent-lane execution: an explicit ROADMAP with parallel lanes (C = client/plugin, S = server/API, R = infra, D = tooling) and a one-step handoff protocol, driving a burst of merged PRs.
- A second music-tooling thread: the **Ableton SDK** Articulation Roll got a full REAPER port, and late in the week a browser **music-theory learning suite** (chord-inversion-trainer) was built from scratch.

## Key learnings
- End-to-end VPS deployment: coturn/TURNS on :5349 with auto cert sync, Caddy + Let's Encrypt for the API, healthz/readyz liveness, minting TURN credentials.
- Fighting a real toolchain: getting iPlug2 to build in CI (macOS VST3+CLAP+standalone green; Linux abandoned — iPlug2 has no Linux support).
- WebRTC clock-sync handshake (clock_ping/clock_pong) and loopback/PDC calibration with a pre-flight gate.
- Offline-first durability — a resumable, drop-proof upload queue (C3) and long-take clock-drift protection via measure + resample-to-fit (C4).
- Auth hardening patterns: email verification, password reset, login rate-limiting.
- Seam-based integration (ITakeUploadStep) to bridge the capture queue to the API upload flow.
- Curriculum design in practice — interleaving + spacing applied to a real guided course track.

## Projects touched
- **remoter-recording** — VPS1 signaling + coturn, VPS2 API (auth/sessions/participants/backing-tracks/storage/email/signal), C0 plugin scaffold, C1 host-transport anchor + FLAC, C2 calibration, C3 offline-first queue, C4 clock-drift, C6 upload wiring, D1 tests+CI, D2 host_info, R1/R3/R4/S1/S2/S4 lanes.
- **Ableton SDK** — Articulation Roll REAPER port (ReaImGui panel, ReaScript + ReaPack index, Ableton feature parity).
- **chord-inversion-trainer** — premium redesign → full music-theory suite → guided course system (14 commits, 07-04).
- **Personal-Spending-Dashboard** — dashboard server + data (minor).

## Shipped / committed
- remoter-recording: PRs #1–#17 across the week — S1/S2 (participants + auth), D1 (CI), C1 (host-transport), C2 (calibration), R1 (VPS1 live), S4/R4 (VPS2 live), R3 (clock-sync), C3 (offline-first), C4 (clock-drift), C6 (upload integration). Both VPSes live (api.imavious.org, signal.imavious.org).
- Ableton SDK: REAPER port with full Ableton feature parity.
- chord-inversion-trainer: 14 commits — redesign, music-theory suite, course system, extended chord tier.

## Open threads & blockers
- remoter branches with commits on origin not yet pulled to local main: `claude/C3-offline-first-capture`, `claude/C4-clock-drift`, `claude/daw-placement-options`.
- 06-29 was an off-day; the sprint front-loaded into 07-01 → 07-04.
